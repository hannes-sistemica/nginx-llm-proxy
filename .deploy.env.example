# Copy to .deploy.env (gitignored) and fill in. Used by `make deploy*`.
DEPLOY_HOST=root@192.168.1.125
PROXY_URL=http://192.168.1.125:4000
ADMIN_PASSWORD=changeme
# MLX Whisper service key — injected into nginx.conf (replaces <WHISPER_API_KEY>)
WHISPER_API_KEY=changeme
